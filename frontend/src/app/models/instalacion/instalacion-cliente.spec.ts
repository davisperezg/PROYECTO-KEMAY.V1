import { InstalacionCliente } from './instalacion-cliente';

describe('InstalacionCliente', () => {
  it('should create an instance', () => {
    expect(new InstalacionCliente()).toBeTruthy();
  });
});
