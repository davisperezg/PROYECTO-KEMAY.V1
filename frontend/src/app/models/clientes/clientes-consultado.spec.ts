import { ClientesConsultado } from './clientes-consultado';

describe('ClientesConsultado', () => {
  it('should create an instance', () => {
    expect(new ClientesConsultado()).toBeTruthy();
  });
});
